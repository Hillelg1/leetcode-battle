# Create User Flow 

#### the following is a flowchart documenting the users creating a new account

```mermaid
sequenceDiagram
    participant DB as Database
    participant Client
    participant Server
        
```